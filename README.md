# Playwright Google Test

![Playwright CI](https://github.com/projectsByBiju/playwright-google-test)

Basic Playwright test to verify Google title.

## Run locally
```bash
pip install playwright pytest
playwright install
pytest tests/



#### 6. Final push & open PR

```bash
git add .
git commit -m "chore: add GitHub Actions CI pipeline + badge"
git push origin feature/add-google-test